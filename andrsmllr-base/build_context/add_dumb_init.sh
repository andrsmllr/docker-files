#!/bin/sh
# dumb-init can be added to a Docker image in different ways:
#   1. install from package manager (not statically linked)
#   2. download statically-linked binary
#   3. compiled from sources
